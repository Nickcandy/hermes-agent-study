# Hermes 源码学习笔记

> 学习方式：组合模式（我带路讲小点 → 你复述 → 考一考）＋ 笔记沉淀
> 原则：翻代码是我的活，你负责听懂、复述、提问。

## 课程索引

### A 阶段 · 会话循环与上下文组装
- [x] 四通道总框架（见下）—— Context 如何进入模型
- [x] 概览：一次轮次 = 四文件接力（run_agent 薄转发 → conversation_loop 指挥 → turn_context 布置 → context_engine 管家）
- [x] prologue 五步：环境布置 → 状态清零 → 对话装配 → 说明书恢复+压缩预检 → 注入与落库
- [x] Context 组装：五源两通道（记忆/插件→用户消息尾部；上下文文件/核心→说明书）+ sidecar 侧车
- [x] 第 1 课：sidecar（api_content）机制
- [ ] 下一课：B 阶段提示词缓存与压缩

---

## A 阶段 · 四通道总框架（总纲）

Context 不是统一的"召回结果"，而是通过**四条通道**进入模型：

| 通道 | 内容 | 更新频率 |
|---|---|---|
| ① System Prompt | 固定规则 + Context Files + Skill 索引 + 内置 Memory 快照 | 会话构建/重建 |
| ② Message History | 历史 user / assistant / tool 消息 | 持续增长/压缩 |
| ③ 当前 User 消息 | 当前问题 + 外部记忆召回 + 插件上下文 + Gateway 说明 | 每轮追加尾部 |
| ④ Tools 参数 | Tool Schema，独立于 messages，走 `tools=` | Toolset 组装时 |

两条铁律：
- **稳定内容 → System Prompt**（缓存前缀，一字不动）
- **动态内容 → 当前 User 消息尾部**（sidecar 保字节稳定）

### 术语 · 什么叫"字节稳定"
- 字节（byte）= 机器存文字的最小单位；稳定 = 不变。
- 大模型缓存判定是"逐字节对比"：前面那段内容只要差一个空格/换行，缓存就失效、重新全价。
- 所以"字节稳定" = 每次请求的前缀逐字节一致，才能白嫖缓存折扣。
- 它是 A 阶段所有设计的共同目标：说明书不重建、sidecar 记精确字节、_stored_prompt_matches_runtime 只在换模型/目录/平台时才重建、发请求前的空白/JSON 规范化。

### 缓存的三块，互不牵连（conversation_loop.py:1213）
- ① system + 历史消息：锁死，逐字节不变（说明书不重建 + sidecar 重放）
- ② 当前 user 消息尾部：每轮新增（问题+记忆+插件）
- ③ tools 定义：独立缓存段（strip_anthropic_tool_cache_control 单独剥离），改 tools 只失效 tools 段，不影响消息前缀

### sidecar 存什么、注入什么
- 存：当前轮消息的"完整发送版" = 干净内容 + "\n\n" + 记忆围栏块 + 插件上下文 的精确字节
- 注入：构建 api_messages 时 `api_msg["content"] = api_content`（用 sidecar 替换 content）
- 存档：干净 content 给 UI/存库；sidecar 一起持久化但不进正文
- **为什么必须两条（不能一条搞定）**：① 只存发送版 → UI 脏（混着 <memory>/插件）+ 下一轮重放会叠加旧注入，越滚越大、字节不稳；② 只存干净版 → 重放时少了注入，前缀变了、缓存失效。干净版用于"重新注入的基底"，发送版用于"历史重放保字节"，两个目标相反，缺一不可。

### Memory 默认是关闭的（重要）
- 外部记忆服务（mem0/honcho…）是**可选功能**，默认不启用。agent_init.py:1726 读 `memory.provider` 配置，为空就整个跳过 → `_memory_manager = None` → 每轮不做 prefetch 召回。
- 内置记忆（MEMORY.md/USER.md）也要 `memory_enabled` 配置开启才 `load_from_disk()`。
- 三个闸门缺一不可：① 配置写 `memory.provider` ② 装插件+API key ③ is_available() 为 True。
- 默认环境：两条记忆路都是关的，Hermes 纯靠 system prompt + 历史对话工作。
- 之前讲的"召回"是"若开启外部记忆才发生"；召回接口在核心（MemoryProvider ABC），算法在插件（mem0=向量/honcho=混合/hindsight=知识图谱…）。

### 压缩触发（三类，不只 token 阈值）
- token 阈值（主要）：用量到 75%（threshold_percent=0.75）
- 闲置触发：隔太久没聊（idle_compact_after_seconds）
- 手动：/compress 命令
- 到阈值也不一定立刻压：有冷却期、最小回收 token 门槛（偶发原则）

---

## B 阶段 · 提示词缓存与压缩

### 缓存断点（cache_control，prompt_caching.py）
- cache_control 标记打在消息某处 = "从这里缓存，下次原样重用免费"。
- 默认 4 个断点：① 静态 system 前缀末尾 ② 说明书末尾 ③ 倒数第2条 ④ 最后1条。无静态前缀时退化为 system + 最后3条。
- 三个精细拆分（精髓："只缓存稳定的部分"）：
  1. **静态前缀拆分**：说明书存库是一整段，发出时拆 [静态前缀(缓存)] + [易变后缀(不缓存)]，静态前缀可跨会话复用（对应 system 三档 stable/volatile）。
  2. **builder 声明前缀**（prompt_cache_boundary.py）：skill/webhook 消息 = 稳定脚手架 + 易变尾部（ticket ID/时间戳），builder 注册稳定前缀边界，规划器据此拆分 → 尾部变不影响脚手架缓存。
  3. **tools 独立缓存**：最后一个 tool 打标记，改 tools 不影响消息前缀。
- 每次请求先 strip 旧标记，再按当前 provider 策略重新 build（_redecorate_prompt_cache_for_provider）。

### 缓存断点"什么时候用"
- 断点是稀缺资源：Anthropic 一次最多 4 个（remaining = 4 - breakpoints_used），要精打细算。
- 什么时候开：`agent._use_prompt_caching` 为 True（Anthropic/OpenRouter 支持才打；OpenAI/DeepSeek 不认 cache_control，不打）。
- 什么时候打、打哪：每次请求前重算（strip→build），分配优先级 system 静态前缀 → 说明书末尾 → 最近 2 条非 system 消息（最近的消息最可能留到下一轮）。
- 什么时候精细拆分：有"稳定长块+易变短尾"时拆开（静态前缀、skill 脚手架），省断点 + 尾部变不影响缓存。
- 什么时候不打：_can_carry_marker 为 False 的消息（空内容、纯 tool_calls 的 assistant），打了白打，主动跳过省名额。
- 什么时候失效：压缩重写历史 / TTL 到期（5m/1h）/ 前缀被改动。

### 两类缓存机制（cache_control 只有 Anthropic 用）
- `cache_control` 显式断点 = Anthropic 专属（Anthropic 原生 + OpenRouter 等兼容网关）。
- 其他 provider 缓存是自动的，不需手动打断点：OpenAI 自动前缀缓存、DeepSeek 上下文硬盘缓存、本地 Ollama/llama.cpp/vLLM 用 KV cache 复用。
- Hermes 两手：对 Anthropic 系打 cache_control；对其他靠"字节稳定"（说明书不重建 + sidecar + 空白/JSON 规范化）让自动缓存命中（conversation_loop.py:2068 注释）。
- 结论：字节稳定是通用地基（所有 provider 缓存都靠它）；cache_control 是 Anthropic 专属的显式提示。

### 压缩（context_compressor.py，前面已详述）
- 触发三类 + 保两头压中间 + 头部衰减 + 偶发破坏缓存。
- 与缓存的关系：压缩是唯一允许破坏缓存的例外，用一次性代价换长期收益。

### 一句话总结
缓存断点 = 精确定位稳定部分去打标；压缩 = 太长时压中间、成为唯一破坏缓存的操作。统率原则：最大化缓存命中，最小化缓存破坏。

### System Prompt 内部三段（system_prompt.py:152）
- `stable`：SOUL.md 身份、Tool/任务/并行调用规则、环境提示
- `context`：调用方 system_message + 工作区快照 + Context Files（.hermes.md → AGENTS.md → CLAUDE.md → .cursorrules，AGENTS.md 会合并整条 Git 目录链，prompt_builder.py:2273）
- `volatile`：Skill 索引、内置 MEMORY.md、USER.md、日期/Session/模型 —— "重建说明书时最可能变的部分"

### 容易混淆的三对概念
1. **内置 Memory vs 外部 Memory**：内置 = MEMORY.md/USER.md 文件快照 → 进 System Prompt（不检索）；外部 = prefetch_all 按当前问题召回 → 进当前 User 消息尾部（算法由 Provider 决定）
2. **Skill 索引 vs 完整 Skill**：索引（name+description）→ System Prompt；完整 SKILL.md → skill_view 的 Tool Result（按需）
3. **Context Files vs 检索召回**：Context Files 是确定性文件发现（固定优先级 + 长度截断），不是 BM25/向量

### 最终请求形态
```python
client.chat.completions.create(
    messages=[system(说明书), user(上一轮+召回), assistant, tool, user(当前+召回+插件)],
    tools=tool_schemas,   # 独立于 messages
)
```

### 上下文完整构成（4 块）
1. **说明书 system prompt**：固定前缀，会话级缓存（内含 skill 索引，不是完整 skill）
2. **历史对话**：默认完整原样（user/assistant/tool）；超长才压缩
3. **当前用户消息**：问题 + 外部记忆召回 + 插件上下文（动态尾巴）
4. **工具定义 tools=**：独立于 messages，单独发送

### 压缩策略：保两头、压中间
- 触发：用量达 75%（threshold_percent=0.75）
- 头部保留前 3 条（protect_first_n=3）—— 保住缓存前缀
- 尾部保留最近 6 条（protect_last_n=6）—— 保住最近上下文
- 中间老对话 → LLM 压成一段摘要

易错点：默认没有"摘要"；压缩时"最近几轮"是完整保留，被压成摘要的是中间陈年旧账。

### 压缩 vs 缓存：偶发破坏 + 滑动窗口（B 阶段铺垫，context_compressor.py）
- 压缩 = 有意、偶发的缓存破坏（AGENTS.md：唯一允许破坏缓存的例外）。一次性代价换长期收益：多花一次压缩的钱，对话变短，之后每轮继续享受缓存。
- 让破坏"偶发"而非"每轮"的机制：75% 阈值、冷却期、最小回收 token 门槛、rearm 间隔；micro-compaction（每轮滚动压缩）默认关闭——注释明说"每轮都打破缓存前缀"是坏事。
- 滑动窗口：protect_last_n=6 是"每次压缩保留当下尾 6"，不是锁定 6 轮；旧尾巴变中间，下次压缩被压进摘要。
- 头部衰减：protect_first_n 第一次压缩后衰减为 0（compression_count>=1，:5145），只剩 system——因为早期轮次已被摘要捕获，继续锁着会"变成化石永远占位置"（注释原文 fossilize，#11996）。

---

## 第 1 课 · sidecar（api_content）机制

### 一句话结论
会变的注入内容（记忆、插件）不直接进正文存档，而是把"拼接后的完整版本"存进 `api_content` 侧车；发请求时用侧车字节，存档时用干净正文 —— 保证每轮重放时前缀一字不差，缓存才能命中。

### 关键代码

**组装（prologue 阶段，turn_context.py:53）**
```python
def compose_user_api_content(content, ext_prefetch_cache, plugin_user_context):
    if not isinstance(content, str):
        return None                    # 多模态等非字符串，原样发送
    injections = []
    if ext_prefetch_cache:             # 记忆预取 → 加"围栏"包裹
        injections.append(build_memory_context_block(ext_prefetch_cache))
    if plugin_user_context:            # 插件上下文 → 原样追加
        injections.append(plugin_user_context)
    if not injections:
        return None                    # 啥都没有就不注入
    return content + "\n\n" + "\n\n".join(injections)   # 拼到用户消息尾部
```

**应用（发送前，conversation_loop.py:1866）**
```python
if idx == current_turn_user_idx and msg.get("role") == "user":
    if isinstance(_api_content, str) and _api_content:
        api_msg["content"] = _api_content   # 本轮：换上拼接后的版本
    # 历史消息（1883 行）：有 api_content 就重放 → 前缀字节稳定
```

### 三个时刻
1. 组装（prologue）：记忆+插件 → 追加到用户消息尾部 → 存 `api_content`
2. 发送（主循环）：`api_messages` 构建时 pop 侧车替换 content
3. 重放（下一轮）：历史消息沿用侧车字节，一字不差

### 为什么这能保缓存
- 说明书（system prompt）不动 → 缓存前缀头部稳定
- 注入点固定在"用户消息尾部" → 每轮前缀的"终点"一致
- 侧车存档 → 重放时字节不变 → 缓存命中率最大化

---

## C 阶段 · 记忆与跨会话学习

### 核心结论
Hermes 的"学习"不是 ML 学习，而是"把会话中的东西沉淀成可复用资产"。learn_prompt.py 原话："There is no separate distillation engine"（没有独立的蒸馏引擎）。

### 三种学习形式
1. **记记忆**（memory 工具）：写 MEMORY.md / USER.md，跨会话记住"你是谁"。
2. **学技能**（/learn）：用现有工具（read_file / web_extract / skill_manage）把材料蒸馏成 SKILL.md，跨会话复用"怎么做"。纯手动触发（CLI /learn、gateway /learn、dashboard 面板），不敲不会自动学。
3. **可视化**（learning_graph.py）：把"学到的技能 + 记忆块"画成图给桌面端学习面板。边来自 related_skills 声明 + 记忆↔技能的词汇重叠（不是语义）。

- 学完技能 = 磁盘上多一个 SKILL.md 文件（已有类似就 extend 不新建），之后进 system prompt 的 skill 索引。
- 记忆是半自动的（agent 主动调 memory 工具），学技能是纯手动的。

### 术语 · 窄腰（narrow waist）
- AGENTS.md 原文："The core is a narrow waist; capability lives at the edges."
- 出处：系统设计的沙漏架构。中间一根极简稳定的"窄腰"（如互联网的 IP 协议），上下两端自由扩展、互不拖累。
- Hermes 的窄腰 = agent 核心（会话循环 + 工具 schema + 几个接口 MemoryProvider/ContextEngine）；边缘 = skill、memory 插件、MCP、平台适配器。
- 为什么"窄"：每加一个核心工具都要在每次 API 调用付代价，所以核心越窄越好，能力下沉到边缘插件。
