# 2026-08-14

## D 阶段（状态持久化与子代理）—— 四阶段闭环

- SessionDB（hermes_state.py 11165 行，全库最大单文件）= SQLite 会话库，三 Mixin 拆分：
  SessionSchemaMixin（schema.py 表结构）/ SessionSearchMixin（search.py FTS5）/ SessionPortabilityMixin（portability.py 导出迁移）。
- 关键表：sessions / messages(api_content=sidecar) / system_prompts(说明书缓存) /
  messages_fts+trigram(全文搜索含中文CJK) / async_delegations(子代理)。
- 底层健壮性：WAL、写锁竞争调优、崩溃修复、错误分类。
- 子代理：delegate_task（进程内 spawn 子 AIAgent）+ delegation_context（ContextVar 隔离身份）+ batch_runner（批量多进程+断点续跑）。
- 洞见：hermes_state 之所以巨，因为它是"边缘能力"（存储/搜索脏活累活），核心反而窄——窄腰完整图景。

## 四阶段完成
A 上下文组装 → B 缓存+压缩 → C 学习 → D 状态持久化+子代理。笔记在 源码学习笔记.md 已补全 D 阶段。

## 敏感信息屏蔽 + 学习收尾
- 敏感信息屏蔽：agent/redact.py（1000+ 行正则脱敏引擎）。守住四个出口（日志 RedactingFormatter / 终端输出 / 读给模型的文件 / 命令行显示）。默认开启（HERMES_REDACT_SECRETS 默认 true，config: security.redact_secrets）。
- 涂掉：40+ 厂商密钥前缀、环境变量 KEY=、JSON/YAML 密码字段、私钥块、数据库连接串、JWT、手机号。
- 三个精妙点：不误杀普通词(Secretary/tokenizer)、防走私(控制字符拆密钥)、防写坏(用占位符不用截断密钥)。
- 唯一豁免：网页 URL query 里的 token（OAuth 回调/魔法链接需要）。
- 学习里程碑：核心已闭环（阶段0+A/B/C/D+敏感信息屏蔽），剩余可选广度：多模型适配器 / gateway 消息平台 / cron 定时 / 桌面端 TUI。
