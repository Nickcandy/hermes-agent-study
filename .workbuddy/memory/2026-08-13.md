# 2026-08-13

## Hermes 源码学习路线梳理

帮用户读完其 `.understand-anything/` 知识图谱笔记（1236 节点 / 2172 边 / 7 层 / 12 步 tour），
`config.json` 设 `outputLanguage: zh`，`.understandignore` 白名单聚焦三条能力链 + 行为测试。

结论：
- 已覆盖：边缘能力链（Tool / Skill / MCP 的注册、发现、安全、执行）+ 契约测试。
- 盲区：`run_agent.py` 的 `run_conversation` 实为 thin forwarder，真正循环体在 `agent/conversation_loop.py`（7757 行）。
- 下一步深扒窄腰核心（4 阶段）：
  A 会话循环与上下文组装 — conversation_loop / context_engine / prompt_builder
  B 提示词缓存与压缩 — prompt_caching / prompt_cache_boundary / context_compressor / conversation_compression
  C 记忆与跨会话学习 — memory_manager / learning_graph / learn_prompt / trajectory_compressor
  D 状态持久化与子代理 — hermes_state*（11165 行）/ delegation_context / batch_runner

建议：把上述新文件补进 `.understandignore` 白名单，重跑 understand-anything 纳图。

## A 阶段深扒（会话循环与上下文组装）

模块分工（指挥者 + 被抽取模块模式）：
- run_agent.py：AIAgent 类（412 行），run_conversation 是薄转发。
- conversation_loop.py（7757）：run_conversation 巨函数（1422 起，~6300 行），前半 ~65 个辅助函数。
  关键辅助函数：_restore_or_build_system_prompt(555)、_stored_prompt_matches_runtime(693)、
  _ensure_cached_system_prompt_static(1179)、_redecorate_prompt_cache_for_provider(1213)、
  _apply_context_engine_selection(1286)、_notify_context_engine_turn_complete(1371)。
  主循环 while 在 1634；interrupt 检查 1649；budget 1656；streaming 决策 ~2625；update_from_response 3620。
- turn_context.py（~1400）：prologue 抽取，build_turn_context(430 起) 做每轮布置（8 项职责）。
- context_engine.py（489）：可插拔上下文管理 ABC，内置实现 ContextCompressor。
- prompt_builder.py（2347）：system prompt 工厂（身份+指导常量+环境探测+skills 清单+上下文文件）。
- 新发现：system prompt 静态前缀重建在独立模块 agent/system_prompt.py（reconstruct_static_prefix）。

两个神圣属性的落点：system prompt 字节稳定（_stored_prompt_matches_runtime 逐字校验）、
cache_control 注入/剥离（_redecorate_prompt_cache_for_provider / strip_anthropic_cache_control）。

## prologue 五步（turn_context.py build_turn_context，430 行起 ~900 行）

① 环境与线程布置：install_safe_stdio、绑定日志会话、恢复主 provider（_restore_primary_runtime）、
   每轮刷新 MCP 工具（refresh_agent_mcp_tools，缓存放行因尚未发请求）。
② 状态清零：清洗 surrogate、重置所有重试计数 + 工具护栏 + 迭代预算、清死 TCP 连接。
③ 对话装配：用户消息入列、从历史恢复 todo/记忆提醒计数、判断 memory nudge。
④ 说明书恢复 + 压缩预检：_cached_system_prompt 为 None 才重建；idle（闲置时长）与
   preflight（rough token 超阈值）两种触发提前压缩，压缩后 reanchor 用户消息位置。
⑤ 注入与落库：pre_llm_call 插件钩子 + 外部记忆 prefetch_all 注入到【用户消息】而非 system prompt；
   api_content 侧车记录"实际发送字节"以便下次逐字重放；首次 LLM 调用前先把用户消息落库（防崩溃）。

洞见：prologue 每一步都在保护缓存前缀 —— 可变内容（记忆/插件）一律注入用户消息，
说明书保持字节稳定；api_content 保证重放时前缀一字不差。

## understand-anything 插件（本地）

仓库：~/.understand-anything/repo/understand-anything-plugin（@understand-anything/skill v2.7.5）
含 8 个 skill：understand / understand-chat / understand-dashboard / understand-diff /
understand-domain / understand-explain / understand-knowledge / understand-onboard。
understand-explain：基于 knowledge-graph.json 对指定文件/函数/模块做深度讲解
（图谱定位 → 关联边/邻居 → 所在层 → 回读源码 → 上下文解释）。用法 /understand-explain <路径>。
另一项目 nano-indexer 也有 .understand-anything 产物。

## 图谱更新（用 /understand skill 流程）

- .understandignore 白名单扩展 +19 核心文件（conversation_loop/turn_context/context_engine/context_compressor/
  context_breakdown/conversation_compression/native_compaction/prompt_builder/system_prompt/prompt_caching/
  prompt_cache_boundary/memory_manager/memory_provider/learn_prompt/learning_graph/learning_mutations/
  delegation_context/batch_runner/trajectory_compressor），根目录放行 batch_runner.py、trajectory_compressor.py。
- 3 个 file-analyzer 子代理产出 batch-101/102/103.json（155 节点 274 边），合并进图谱：
  1236→1380 节点、2172→2446 边；node type 统一 func→function；layer:agent-core +19 文件节点；校验通过。
- 图谱揭示的结构事实：conversation_loop 中枢 imports 9 个核心模块；context_compressor 与
  conversation_loop/turn_context 存在循环依赖（用 depends_on 规避 import cycle）；
  缓存三件套 system_prompt→prompt_builder、prompt_caching→prompt_cache_boundary；
  记忆链 turn_context→memory_manager→memory_provider；batch_runner→delegation_context。

## 学习路线 + A/B/C 阶段教学 + skill 沉淀

- 今天完成 A（会话循环与上下文组装）、B（缓存断点+压缩）、C（记忆与跨会话学习）三阶段教学。
  核心结论：字节稳定是通用缓存地基；cache_control 是 Anthropic 专属；压缩=偶发破坏缓存+滑动窗口+头部衰减；
  学习=沉淀资产（无 ML 学习引擎）；窄腰=narrow waist（沙漏架构，核心极简、能力在边缘）。
- 用户讲解偏好（已写入 ~/.workbuddy/MEMORY.md）：少堆英文标识符、别太碎、关键代码贴对话、澄清易混概念、用比喻。
- 生成 skill：source-code-guide（源码带读方法论），位于 ~/.workbuddy/skills/source-code-guide/SKILL.md。
  内容：金字塔四步（摸结构→总框架→分层→表格总结）+ 讲解原则 + 节奏验证。frontmatter 含 agent_created: true。
- 学习笔记在项目根 源码学习笔记.md（A/B/C 阶段 + 四通道 + sidecar + 缓存 + 学习机制 + 窄腰）。
- 已提交并推送到 origin（Nickcandy/hermes-agent-study），commit 9733b21，8 文件；.gitignore 加了 .understand-anything/intermediate/ 排除中间产物。注意：git 身份未配置（committer 自动生成为 nickcandy@nicks-MBP.lan）。
