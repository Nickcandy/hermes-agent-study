# Hermes 源码复习大纲

> 用法：每块先自己复述，再看要点查漏。按「阶段 0 → A → B → C → D」顺序过。

---

## 阶段 0 · 三条能力链（边缘：能力如何接入核心）

### Tool 运行时链
- **registry**：中央注册表，存每个工具的 schema、handler、元数据、可用状态；声明/发现/执行三者解耦
- **tool_search**：渐进式披露，把大量插件/MCP 工具折叠成可搜索目录，避免撑爆上下文
- **tool_dispatch_helpers**：工具调用的分批、并行冲突判断、结果整理
- **tool_guardrails**：拦截重复失败、无进展循环、超限调用
- **file_tools / terminal_tool**：文件与终端能力

### Skill 运行时链
- **skill_utils**：统一解析元数据、路径、来源优先级
- **skills_tool**：在内置/用户/插件/组织目录间发现 skill，`skills_list`/`skill_view` 安全展示
- **skill_commands**：把 skill 暴露为斜杠命令
- **skill_preprocessing**：模板变量、配置、受控 shell 展开
- **skill_bundles**：多个 skill + 附加指令组合成用户消息
- **skills_guard / skills_hub**：安装前扫描危险结构；多来源搜索/隔离/安装/审计

### MCP 接入链
- **mcp_config**：服务增删、探测、登录、持久化
- **mcp_security**：配置落地前识别危险命令、内联脚本、异常 URL
- **mcp_catalog**：审核后的可选安装入口
- **mcp_startup**：后台预热发现，避免启动被远端阻塞
- **mcp_tool**：数据面主干——建连接、发现、schema 转换、转发、断线重连
- **mcp_oauth / mcp_stdio_watchdog**：认证恢复、子进程生命周期

### 行为验证
- 契约测试覆盖 toolsets / skills / mcp 的关键不变量（缓存隔离、安全边界、并发恢复）

---

## 阶段 A · 上下文组装（核心：请求怎么拼出来）

### 四文件接力
- **run_agent**：AIAgent 类（:412），`run_conversation` 是甩手掌柜（自己不干活，转手交别人）
- **conversation_loop**：主循环指挥者（:1422 run_conversation，一个函数写了几千行）
- **turn_context**：每轮开场布置（:430 build_turn_context）
- **context_engine**：能换的上下文接口（ABC），内置 ContextCompressor
- **prompt_builder**：system prompt 工厂

### prologue 五步
环境布置 → 状态清零 → 对话装配 → 说明书恢复+压缩预检 → 注入与落库

### Context 四通道
① System Prompt（稳定） ② Message History ③ 当前 User 消息（动态） ④ Tools 参数（独立）

### sidecar（api_content）
- 两条：`content`（干净版，给 UI + 当重新注入的基底）+ `api_content`（发送版，重放保字节）
- 为什么不能一条：只存发送版→UI 脏+注入叠加；只存干净版→重放缺注入、缓存失效

### 字节稳定
- 定义：每次请求的前缀逐字节一致，差一个空格缓存就失效
- 它是所有 provider 缓存的通用地基

### system prompt 三档
- **stable**：身份/规则/环境（跨会话不变）
- **context**：调用方 message + 工作区 + Context Files（换项目才变）
- **volatile**：skill 索引/记忆/日期/模型（最易变）

---

## 阶段 B · 缓存 + 压缩

### 缓存断点（cache_control）
- 默认 4 个：① 静态前缀末尾 ② 说明书末尾 ③ 倒数第2条 ④ 最后1条
- 稀缺资源，精打细算；每次请求 strip 旧的 + build 新的
- 三个精细拆分：静态前缀 / builder 声明的脚手架 / tools 独立缓存

### 两类缓存机制
- `cache_control` 显式断点 = Anthropic 专属
- OpenAI/DeepSeek/本地 = 自动前缀缓存 / KV cache，靠"字节稳定"命中

### 压缩
- 触发三类：token 75% / 闲置 / 手动 /compress
- 策略：保两头压中间（protect_first_n=3 / protect_last_n=6）
- 头部衰减：第一次压缩后 protect_first_n 衰减为 0（避免化石）
- 与缓存关系：偶发破坏缓存（一次性代价换长期收益）

---

## 阶段 C · 学习（跨会话沉淀）

### 三种学习形式
- **memory 工具** → MEMORY.md/USER.md（记用户信息）
- **/learn** → SKILL.md（学技能，纯手动触发）
- **learning graph** → 可视化（技能+记忆画成图）

### /learn 本质
- 没有独立蒸馏引擎，agent 用现有工具（read_file/skill_manage）把材料写成 skill

### 窄腰（narrow waist）
- "The core is a narrow waist; capability lives at the edges."
- 核心极简稳定，能力在边缘插件（MemoryProvider/ContextEngine 都是"接口在核心、实现在插件"）

---

## 阶段 D · 持久化 + 子代理

### SQLite 会话库（SessionDB）
- 三 Mixin：Schema（表结构）/ Search（FTS5）/ Portability（导出迁移）
- 关键表：sessions / messages(api_content) / system_prompts / session_model_usage / async_delegations / messages_fts
- 底层：WAL 模式、写锁竞争调优、崩溃修复

### FTS 三连
- **messages_fts**：FTS5 虚拟表，全文索引（content='messages' 外部内容表）
- **cjk**：中日韩，unicode61 切成单字导致短语失效
- **trigram**：3 字节重叠分词，支持任意子串（专为 CJK 建 messages_fts_trigram）

### 子代理委派（delegate_task）
- LLM 决定「拆几个」（tasks 数组），代码决定「怎么跑」（tasks>1 就并行）
- 依赖判断靠 LLM 语义，代码不兜底
- role（leaf/orchestrator）+ max_spawn_depth（默认2）+ max_concurrent_children
- delegation_context 用 ContextVar 隔离子代理身份
- 聚合：join 所有子代理，拼成一个结果 JSON
- batch_runner：多进程批量 + checkpoint 断点续跑

---

## 贯穿全程的一句话

**阶段 0 = 边缘进料（能力注册），A/B/C/D = 核心加工（组装→缓存→学习→持久化）。核心是窄腰，能力在边缘。**
